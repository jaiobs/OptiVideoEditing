<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/croppingImageContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.litpic.litpicsdkdroid.imageeditor.customview.CustomImageView
        android:id="@+id/cropping_image_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true" />

    <FrameLayout
        android:id="@+id/tag_tans_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        android:visibility="invisible">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:textColor="@color/white"
            android:background="@drawable/bg_rounded_corner"
            android:backgroundTint="@color/black_shade"
            android:gravity="center"
            android:padding="10dp"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:text="@string/tag_user_tap_screen_msg"
            android:textSize="18sp" />
    </FrameLayout>

</RelativeLayout>
